import { DescriptedCode } from '@smartb/g2-documentation'
import RequirementApiDescLeft from './descLeft.md'
import RequirementApiDescRight from './descRight.md'

<DescriptedCode
  leftElement={<RequirementApiDescLeft />}
  leftContainerProps={
    {
      width: "50%"
    }
  }
  rightElement={<RequirementApiDescRight />}
  rightContainerProps={
    {
      width: "50%",
      position: "relative",
      paddingTop: "0"
    }
  }
/>

---
