import { DescriptedCode } from '@smartb/g2-documentation'
import RequestApiDescLeft from './descLeft.md'
import RequestApiDescRight from './descRight.md'

<DescriptedCode
  leftElement={<RequestApiDescLeft />}
  leftContainerProps={
    {
      width: "50%"
    }
  }
  rightElement={<RequestApiDescRight />}
  rightContainerProps={
    {
      width: "50%",
      position: "relative",
      paddingTop: "0"
    }
  }
/>

---
