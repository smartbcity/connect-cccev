server:
  port: 8083

springdoc:
  api-docs:
    path: /api-docs

spring:
  codec:
    max-in-memory-size: 10MB
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB
      max-request-size: 10MB

  neo4j:
    uri: neo4j://localhost:7687
    authentication:
      username: neo4j
      password: smartbsmartb